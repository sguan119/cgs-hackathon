# Thesis Memory screenshots

Two PNGs drop here pre-demo (§3.A content-owner deliverable):

- `before-m1.png` — Thesis Memory snapshot captured right before Meeting 1.
- `before-m2.png` — Same snapshot captured before Meeting 2, with the diff highlighted in crimson.

Until the PNGs land, `ThesisMemoryToggle` renders the SVG placeholders in this same directory via an `<img onError>` fallback. Both show the same three-column layout (Dominant Logic · Cited Anchors · Open Contradictions) so the Dashboard surface reads coherently in dry-runs.

Swap path: overwrite the PNG files — no code change needed.
